# استخدم نسخة PHP CLI مع خادم ويب مدمج
FROM php:8.2-cli

# أنشئ مجلد العمل داخل الحاوية
WORKDIR /app

# انسخ جميع ملفات المشروع إلى الحاوية
COPY . .

# اكشف المنفذ 10000 للاتصال من Render
EXPOSE 10000

# أمر تشغيل الخادم المدمج على Render
CMD ["php", "-S", "0.0.0.0:10000", "-t", "."]
